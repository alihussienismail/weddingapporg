@page
@model weddingapporg.Pages.Admin.UsersModel
@{
    ViewData["Title"] = "Manage Users";
    Layout = "/Pages/Shared/AdminLTE/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">All Users</h3>
        <a asp-page="/Admin/AddUser" class="btn btn-primary float-right">Add New User</a>
    </div>
    <div class="card-body">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Name</th>
                    <th>Phone</th>
                    <th>Admin</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model.Users)
                {
                    <tr>
                        <td>@user.Email</td>
                        <td>@user.FName @user.LName</td>
                        <td>@user.PhoneNumber</td>
                        <td>@(user.IsAdmin ? "Yes" : "No")</td>
                        <td>
                            <form method="post" asp-page-handler="ToggleAdmin" asp-route-userId="@user.Id" style="display:inline">
                                <button type="submit" class="btn btn-sm btn-warning">@(user.IsAdmin ? "Remove Admin" : "Make Admin")</button>
                            </form>
                            <form method="post" asp-page-handler="Delete" asp-route-userId="@user.Id" style="display:inline" onsubmit="return confirm('Are you sure you want to delete this user?');">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>